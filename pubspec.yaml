name: project_17_6
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Loading Animation
  loading_animation_widget:
  
  # SharedPreferences
  shared_preferences: ^2.5.3
  # Utility & Core
  uuid: ^4.5.1
  intl: ^0.20.2

  # UI/Animations
  animations: ^2.0.11
  flutter_staggered_animations: ^1.1.1
  cupertino_icons: ^1.0.8

  # Navigation & State Management
  get: ^4.6.6

  # Device & System
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: true
  ios: true
  image_path:

# Override transitive dependencies to latest
dependency_overrides:
  characters: ^1.4.1
  material_color_utilities: ^0.13.0

flutter:
  uses-material-design: true
